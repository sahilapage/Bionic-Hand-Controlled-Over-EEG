# Media

Images and video used by the READMEs.

| file | what it shows |
|---|---|
| `image.png` | the hand |
| `hand-pose-overlay.png` | HAMER mesh rendered over an RGB frame |
| `retarget-peace.jpg` | peace sign — human pose beside the retargeted robot pose |
| `retarget-fist.jpg` | closed fist, likewise |
| `retarget-open.jpg` | open hand, likewise |

Add new media here and reference it by relative path. Keep stills as PNG or
JPEG; for clips, prefer a short MP4 over an animated GIF — GitHub renders both,
but an MP4 of the same clip is roughly an order of magnitude smaller.
